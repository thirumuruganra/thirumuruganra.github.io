# Pixel Portfolio

This project is a pixel-art portfolio site built with React, Vite, and TanStack Start. Instead of a plain landing page, it behaves like a tiny desktop OS: open windows, browse projects, read the about panel, grab the resume, and send a note.